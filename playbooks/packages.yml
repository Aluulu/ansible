---
- name: Install common packages
  hosts: all
  become: true

  tasks:
    - name: Install common packages
      ansible.builtin.package:
        name:
          - fish
          - git
          - curl
          - wget
          - tree
          - btop
          - nano
          - starship
          - fastfetch
        state: present
